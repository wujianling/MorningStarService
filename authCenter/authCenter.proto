syntax = "proto3";

package authCenter;
option go_package="./authCenter";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service AuthCenter {
  rpc Ping(Request) returns(Response);
}
